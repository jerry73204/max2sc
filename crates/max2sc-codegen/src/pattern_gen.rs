//! Pattern generation

use crate::CodegenError;
use max2sc_max_types::MaxPatch;
use max2sc_sc_types::Pattern;

pub fn generate_patterns(_patch: &MaxPatch) -> Result<Vec<Pattern>, CodegenError> {
    todo!("Implement pattern generation")
}
